/**
 * The product name, in one place.
 *
 * The mobile app ships as Plumb. The web app is still PortfolioOS PK, so the
 * two names coexist deliberately rather than by oversight.
 */
export const APP_NAME = "Plumb";

/** The one-line promise under the wordmark on the login screen. */
export const APP_PROMISE =
  "Import a broker statement and Plumb tracks holdings, dividends and performance against the KSE-100. It never asks for brokerage credentials and it never places orders.";

export const DISCLAIMER_SHORT =
  "For personal portfolio tracking and research support only. It is not financial advice.";
