// Moved to @psx/shared so the mobile app compiles the same source.
// This re-export keeps existing "@/lib/company/types.ts" imports working; new code should
// import from "@psx/shared/company/types" directly.
export * from "@psx/shared/company/types";
