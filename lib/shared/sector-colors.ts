// Moved to @psx/shared so the mobile app compiles the same source.
// This re-export keeps existing "@/lib/shared/sector-colors.ts" imports working; new code should
// import from "@psx/shared/sector-colors" directly.
export * from "@psx/shared/sector-colors";
