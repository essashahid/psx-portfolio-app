// Moved to @psx/shared so the mobile app compiles the same source.
// This re-export keeps existing "@/lib/config/features.ts" imports working; new code should
// import from "@psx/shared/features" directly.
export * from "@psx/shared/features";
